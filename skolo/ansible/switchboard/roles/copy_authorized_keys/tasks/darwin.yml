- name: Copy authorized_keys to Darwin
  copy:
    src: '{{ repo_root }}/skolo/authorized_keys'
    dest: '/Users/{{ skolo_account }}/.ssh/authorized_keys'
    owner: '{{ skolo_account }}'
    mode: 0644
