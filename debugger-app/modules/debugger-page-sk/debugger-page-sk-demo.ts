import './index';

// Since this is the only module which can load a file, puppeteer screenshot tests of all modules
// go through it.


// TODO(nifong): load a test file in the application.

// document
//   .querySelector('histogram-sk')!
//   .addEventListener('some-event-name', (e) => {
//     document.querySelector('#events')!.textContent = JSON.stringify(
//       e,
//       null,
//       '  '
//     );
//   });