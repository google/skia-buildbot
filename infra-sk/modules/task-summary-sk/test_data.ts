import { TaskSummary } from './index';

export const taskSummaryData: TaskSummary = {
  Analysis: 'There are two failed tests which caused this task to fail.',
  ErrorMessage: 'FAILED: some-test, some-other-test',
};
