#include <tunables/global>

/tmp/skia/skia/out/Static/fiddle flags=(attach_disconnected,mediate_deleted) {
  #include <abstractions/base>

  deny network raw,
  deny network packet,
  file,
  mount,
}
