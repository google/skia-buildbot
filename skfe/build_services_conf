#!/bin/bash

kubectl get services -o json | jq '[
    .items[]
    | select(.spec.ports[].name == "http")
    | del(.spec.ports[]
    | select(.name != "http"))
    | { name: .metadata.name , port: .spec.ports[0].port}
    ]' \
    | gomplate -c - --file=nginx_template.txt
