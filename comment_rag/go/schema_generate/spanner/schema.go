package spanner

// Generated by //go/sql/exporter/
// DO NOT EDIT

const Schema = `CREATE TABLE IF NOT EXISTS CLInfo (
  project STRING(MAX) NOT NULL,
  repo STRING(MAX) NOT NULL,
  change_id INT64 NOT NULL,
  cl_subject STRING(MAX),
  cl_description STRING(MAX)
) PRIMARY KEY (project, repo, change_id);
CREATE TABLE IF NOT EXISTS CommentHistory (
  id STRING(MAX) PRIMARY KEY,
  project STRING(MAX) NOT NULL,
  repo STRING(MAX),
  category STRING(MAX) NOT NULL,
  change_id INT64 NOT NULL,
  file_path STRING(MAX),
  comment_text STRING(MAX),
  code_snippet STRING(MAX),
  analysis STRING(MAX),
  embedding ARRAY<FLOAT32>(vector_length=>768) NOT NULL,
  CONSTRAINT FK_CommentHistory_CLInfo FOREIGN KEY (project, repo, change_id) REFERENCES CLInfo (project, repo, change_id)
);
CREATE VECTOR INDEX IF NOT EXISTS CommentHistoryEmbeddingIndex on CommentHistory (embedding) OPTIONS (distance_type='COSINE');
`

var CLInfo = []string{
	"project",
	"repo",
	"change_id",
	"cl_subject",
	"cl_description",
}

var CommentHistory = []string{
	"id",
	"project",
	"repo",
	"category",
	"change_id",
	"file_path",
	"comment_text",
	"code_snippet",
	"analysis",
	"embedding",
	"CONSTRAINT",
	"VECTOR",
}
