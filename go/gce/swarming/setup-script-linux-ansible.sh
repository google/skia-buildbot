#!/bin/bash

# For machines configured via Ansible, no setup steps are necessary. We keep this script around in
# case that ceases to be true in the future.

set -e -x

echo "Nothing to do; please run the //skolo/ansible/switchboard/linux.yml Ansible playbook."
