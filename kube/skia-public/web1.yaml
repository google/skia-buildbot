apiVersion: apps/v1beta1
kind: Deployment
metadata:
  name: web
spec:
  template:
    metadata:
      labels:
        app: web
    spec:
      containers:
      - name: web
        image: gcr.io/google-samples/hello-app:1.0
        ports:
          - name: http
            containerPort: 8080
        resources:
          requests:
            memory: "30Mi"
            cpu: "10m"
---
apiVersion: v1
kind: Service
metadata:
  labels:
    app: web
  name: web
spec:
  ports:
    - name: http
      protocol: TCP
      port: 8080
  selector:
    app: web
  type: NodePort

