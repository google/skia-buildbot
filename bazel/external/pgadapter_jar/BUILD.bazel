load("@rules_go//go:def.bzl", "go_library")
load("//bazel/external:runfile_paths.bzl", "generate_go_runfile_path")

gen_filename, data = generate_go_runfile_path(
    name = "generate_go_runfile_path",
    go_library_name = "pgadapter_jar",
    path = "pgadapter.jar",
    platform_mapping = {
        "//conditions:default": "@pgadapter//:all_files",
    },
)

# gazelle:ignore
go_library(
    name = "pgadapter_jar",
    srcs = [
        "pgadapter.go",
        gen_filename,
    ],
    data = data,
    importpath = "go.skia.org/infra/bazel/external/pgadapter_jar",
    visibility = ["//visibility:public"],
    deps = ["//bazel/go/bazel"],
)
